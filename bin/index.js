const clearConsole = require('../utils/clearConsole');
const startMessage = require('../utils/startMessage');
const { log } = console;

const run = async () => {
  clearConsole();
  log(startMessage);

  // run ls and get the current directory files
};

run();
